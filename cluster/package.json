{
  "scripts": {
    "start": "node --env-file=.env build/index",
    "build": "tsc",
    "restart": "npm run build && npm run start",
    "clean": "rm -r build/"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "@types/express-jwt": "^6.0.4",
    "@types/node": "^25.0.10",
    "express": "^5.2.1",
    "express-jwt": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
