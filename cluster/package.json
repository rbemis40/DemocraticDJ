{
  "scripts": {
    "start": "node --env-file=.env build/cluster/index",
    "dev": "node --env-file=.env.local build/cluster/index",
    "build": "tsc",
    "restart": "npm run build && npm run start",
    "clean": "rm -r build/"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "@types/express-jwt": "^6.0.4",
    "@types/node": "^25.0.10",
    "dockerode": "^4.0.9",
    "express": "^5.2.1",
    "express-jwt": "^8.5.1",
    "jsonwebtoken": "^9.0.3",
    "typescript": "^5.9.3",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.1",
    "@types/ws": "^8.18.1"
  }
}
