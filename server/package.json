{
  "scripts": {
    "start": "node --env-file=.env build/server",
    "build": "tsc",
    "restart": "npm run build && npm run start",
    "clean": "rm -r build/"
  },
  "dependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/express-ws": "^3.0.5",
    "ajv": "^8.17.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-ws": "^5.0.2",
    "ws": "^8.18.3"
  }
}
