{
  "scripts": {
    "start": "node --env-file=.env build/game_server/index",
    "build": "tsc",
    "restart": "npm run build && npm run start",
    "clean": "rm -r build/"
  },
  "dependencies": {
    "@types/node": "^25.0.10",
    "@types/ws": "^8.18.1",
    "ajv": "^8.17.1",
    "jsonwebtoken": "^9.0.3",
    "typescript": "^5.9.3",
    "ws": "^8.19.0"
  },
  "type": "module",
  "devDependencies": {
    "@types/ajv": "^0.0.5"
  }
}
